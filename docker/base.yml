version: "2"

services:

  app_backend_test:
    # If there is no image, build one
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - DJANGO_ENV=test
        - http_proxy=http://E10356:Jcbb239024@10.162.64.36:8080
        - https_proxy=http://E10356:Jcbb239024@10.162.64.36:8080
    env_file: .env
    network_mode: "bridge"
    restart: always
    volumes:
      - ../backend:/backend
    logging:
        driver: "json-file"
        options:
            max-file: 5
            max-size: 10m
