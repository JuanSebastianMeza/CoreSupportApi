version: "2"

services:

  plantilla_backend:
    # If there is no image, build one
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - DJANGO_ENV=test
        - http_proxy=http://E10356:Jcbb239003@10.162.64.36:8080
        - https_proxy=http://E10356:Jcbb239003@10.162.64.36:8080
    env_file: .env
    restart: always
    volumes:
      - ../backend:/backend
    logging:
        driver: "json-file"
        options:
            max-file: "5"
            max-size: "10m"
